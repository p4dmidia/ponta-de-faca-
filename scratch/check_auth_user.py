import urllib.request
import json

supabase_url = "https://clnuievcdnbwqbyqhwys.supabase.co"
key = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImNsbnVpZXZjZG5id3FieXFod3lzIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3MjExNDkzMCwiZXhwIjoyMDg3NjkwOTMwfQ.2c3qA3jew8xedEzEA_BvXKQgS2BqC1fN5Y0PKb1JKbk"

def query_rest(path, params=""):
    url = f"{supabase_url}/{path}?{params}"
    req = urllib.request.Request(url)
    req.add_header("apikey", key)
    req.add_header("Authorization", f"Bearer {key}")
    try:
        with urllib.request.urlopen(req) as response:
            return json.loads(response.read().decode('utf-8'))
    except Exception as e:
        return str(e)

print("--- AUTH USER FOR FLAVIO ---")
# Try to query auth.users via REST (often works with service_role if exposed)
auth_user = query_rest("auth/v1/admin/users/b041c82b-3ad0-4840-b601-2649706e795b") # Correct admin path for single user
# Wait, auth/v1/admin/users is for GET all. For single it's just ID.
print(json.dumps(auth_user, indent=2))
